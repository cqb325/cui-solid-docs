import { Space, Icon } from 'cui-solid';

export default () => {
    return <Space dir="h">
        <div>
            <Icon name="cog" spin/>
            <Icon name="loader" spin/>
        </div>
    </Space>
}